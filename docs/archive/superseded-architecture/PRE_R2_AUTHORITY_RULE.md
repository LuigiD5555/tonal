# Pre-R2 authority note

Before Architecture R2, several root and experiment-local documents defined Tonal primarily as a composition layer or described runtime authority only as a T1 addendum.

Architecture R2 supersedes those global definitions while preserving frozen T1 experiment authority for T1 itself.

Use Git history for exact pre-R2 text.
