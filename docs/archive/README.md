# Tonal documentation archive

Files under `docs/archive/` preserve historical architecture, nomenclature, experiment context and superseded operational documents.

They are not current architectural authority.

Use them to reconstruct project evolution, provenance or rationale. Do not implement from an archived document unless a current document explicitly references it.

Authority order is defined in the repository root `CLAUDE.md`.
